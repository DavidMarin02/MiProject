// Código para verificar si una cadena es un palíndromo
let inputString = prompt("Ingrese una cadena:");
let isPalindrome = checkPalindrome(inputString);
document.getElementById("palindrome-result").innerText = "Es un palíndromo: " + isPalindrome;

function checkPalindrome(str) {
    // Lógica para verificar si la cadena es un palíndromo
    // Devuelve true si es un palíndromo, false en caso contrario
}
